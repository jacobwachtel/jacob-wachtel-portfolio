import React from 'react';

const ExperienceBullets = ({ bullet }) => {
   return (
      <ul className="bullets__list--items">
         <li>{bullet}</li>
      </ul>
   );
};

export default ExperienceBullets;
